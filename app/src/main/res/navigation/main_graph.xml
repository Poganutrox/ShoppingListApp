<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_graph"
    app:startDestination="@id/homeFragment">
    <fragment
        android:id="@+id/homeFragment"
        android:name="edu.miguelangelmoreno.shoppinglistapp.ui.fragments.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/accountFragment"
        android:name="edu.miguelangelmoreno.shoppinglistapp.ui.fragments.account.AccountFragment"
        android:label="fragment_account"
        tools:layout="@layout/fragment_account" />
    <fragment
        android:id="@+id/productsFragment"
        android:name="edu.miguelangelmoreno.shoppinglistapp.ui.fragments.products.ProductsFragment"
        android:label="fragment_products"
        tools:layout="@layout/fragment_products">
        <action
            android:id="@+id/action_productsFragment_to_filtersFragment"
            app:destination="@id/filtersFragment"
            app:enterAnim="@anim/nav_default_pop_enter_anim" />
        <argument
            android:name="productName"
            app:argType="string"
            app:nullable="true"
            android:defaultValue='' />
        <argument
            android:name="categoryId"
            app:argType="integer"
            android:defaultValue="-1" />
        <argument
            android:name="favItems"
            android:defaultValue="false"
            app:argType="boolean" />
        <argument
            android:name="onSale"
            android:defaultValue="false"
            app:argType="boolean" />
        <argument
            android:name="mercadonaId"
            app:argType="integer"
            android:defaultValue="-1" />
        <argument
            android:name="diaId"
            app:argType="integer"
            android:defaultValue="-1" />
        <argument
            android:name="alcampoId"
            app:argType="integer"
            android:defaultValue="-1" />
        <argument
            android:name="consumId"
            app:argType="integer"
            android:defaultValue="-1" />
    </fragment>
    <fragment
        android:id="@+id/salesFragment"
        android:name="edu.miguelangelmoreno.shoppinglistapp.ui.fragments.sales.SalesFragment"
        android:label="fragment_sales"
        tools:layout="@layout/fragment_sales" />
    <fragment
        android:id="@+id/filtersFragment"
        android:name="edu.miguelangelmoreno.shoppinglistapp.ui.fragments.filters.FiltersFragment"
        android:label="fragment_filters"
        tools:layout="@layout/fragment_filters" >
        <action
            android:id="@+id/action_filtersFragment_to_productsFragment"
            app:destination="@id/productsFragment" />
    </fragment>
</navigation>